seconds = int(input())
hour = seconds // 3600
minute = (seconds - hour * 3600) // 60
sec = (seconds - hour * 3600) % 60
print(seconds, 'секунд - это', seconds // 3600, 'час', minute, 'минут', sec, 'сек')
